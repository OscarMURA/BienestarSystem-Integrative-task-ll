package model;

// The code snippet is defining an enumeration called `TypeStates`. An enumeration is a special type in
// Java that represents a fixed set of constants. In this case, the `TypeStates` enumeration represents
// different states of weight.
public enum TypeStates {
     LOW_WEIGHT,
     NORMAL_WEIGHT,
     OVERWEIGHT,
     OBESITY,
     MORBID_OBESITY, UNKNOWN;
}
